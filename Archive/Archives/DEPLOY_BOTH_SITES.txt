DEPLOY BOTH SITES FROM SAME FOLDER
==================================

oak-valley-estates.pages.dev and ovesubs.pages.dev must show the same selections.

DONE:
  - deploy_live/selections.html and deploy_full_23_review/selections.html are identical
  - Both use Supabase as single source of truth; sync every 30 seconds
  - wrangler.toml set: pages_build_output_dir = "./oak-valley-estates-pages"

NEXT: In Cloudflare Pages dashboard, set BOTH projects:
  - Build output directory: oak-valley-estates-pages
  - Build command: (leave empty, or "echo ok")
  - Redeploy both

They will share data via Supabase.
